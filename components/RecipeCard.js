export default function RecipeCard({ recipe }) {
    return (
        <div>
            {recipe.fields.title}
        </div>
    )
}
// rfc - cria o componente
// parei no minuto 6:00 do video #4 next.js & Contentful tutorial